version: '3'

services:
  
  g-broker:
    build:
      context: ./../g-broker
      dockerfile: ./../g-broker/Dockerfile
    restart: always
    ports:
      - "8080:80"
    deploy:
      mode: replicated
      replicas: 1
